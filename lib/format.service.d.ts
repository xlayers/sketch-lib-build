export declare class FormatService {
    indent(n: number, content: string): string;
    indentFile(n: number, contents: string): string[];
    className(name: string): string;
    normalizeName(name: string): string;
}
